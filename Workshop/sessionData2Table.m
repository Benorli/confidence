function T = sessionData2Table(sessionData)
% Function to convert a sessionData structure (from 2AFC task) into a data
% table with useful data points for analysis

% This function serves as a wrapper, handling file IO and determining
% appropriate conversion functions

% TODO: Everything